import {ActivityCalendar} from "./ActivityCalendar.tsx";

export const Activity = () => {
    return (
        <section className={'activity'}>
            <ActivityCalendar/>
        </section>
    );
}